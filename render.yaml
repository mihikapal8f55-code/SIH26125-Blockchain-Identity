services:
  - type: web
    name: sih26125-blockchain-identity
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app:app" -b 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 300
    envVars:
      - key: PORT
        value: 10000